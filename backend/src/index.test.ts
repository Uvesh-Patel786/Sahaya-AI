import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("index", () => {
  it("should be importable", () => {
    assert.ok(true, "Module should load without errors");
  });
});
